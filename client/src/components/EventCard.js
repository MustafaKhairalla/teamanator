import React, { Component } from 'react';
import {
    Card, Form, FormGroup, Input, Label, ListGroupItem, CardTitle, CardText, CardBody
  } from 'reactstrap';

class EventCard extends Component {

    render() {
        return (

        )
    }
};

export default EventCard;